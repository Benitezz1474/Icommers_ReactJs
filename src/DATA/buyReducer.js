export const buyReducer=(state, action)=>{

    switch(style.type){

        case "BUY" : return [...state,];

        case "BUY_CANCEL" : return [...state];
        
        default : return state
    }
}